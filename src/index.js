import BlocklyDrawer from './BlocklyDrawer';
import * as tags from './ToolBoxTagsComponents';

export const Xml = tags.Xml;
export const Block = tags.Block;
export const Category = tags.Category;

export default BlocklyDrawer;
